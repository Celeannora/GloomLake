# Runtime dependencies
requests>=2.28.0
sqlalchemy>=2.0.0
pyyaml>=6.0.0
rich>=13.0.0

# Optional: GUI scaffold (scaffold_gui.py)
# PySide6>=6.5.0
# Pillow>=10.0.0          (mana icons — PIL fallback circles)
# cairosvg>=2.7.0         (mana icons — full SVG rendering)

# Optional: for development / linting / tests
# ruff>=0.4.0
# mypy>=1.0.0
# pytest>=8.0.0
